echo "🧪 Running unit tests before push..."
npm test -- --run || exit 1
echo "✅ Unit tests passed! Pushing to remote..."
echo "💡 Run 'npm run test:e2e' manually to verify E2E tests before deploying."
